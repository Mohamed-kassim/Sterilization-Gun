import React from "react";
import {ProductSpecs as StyledProductSpecs} from '_molecules'
const ProductSpecs = ({ specs }) => {

  return (
   <StyledProductSpecs specs={specs}/>
  // null
  );
};

export default ProductSpecs;
